# Pinned upstream checkout + its Maven build output — recreated by scripts/build-sut.ps1.
# Upstream source is never committed here; the pin (commit SHA) lives in that script.
/target-app/

# Node toolchain
node_modules/

# Test output (Serenity BDD artefacts + generated report)
/target/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
