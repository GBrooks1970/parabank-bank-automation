{
  "name": "parabank-bank-automation",
  "version": "0.2.0",
  "private": true,
  "description": "Test automation against the ParaBank demo bank: API-first screenplay BDD lane (Phase 2) with a Serenity/JS UI lane to follow (Phase 3).",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint:tags": "node scripts/check-tags.mjs",
    "test:api": "cucumber-js",
    "verify": "npm run typecheck && npm run lint:tags && npm run test:api"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.9.0",
    "@types/node": "^26.1.1",
    "ajv": "^8.20.0",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
